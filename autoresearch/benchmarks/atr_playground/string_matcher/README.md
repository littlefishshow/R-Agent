# string_matcher

Toy autoresearch project for multi-pattern substring matching with overlapping counts. Baseline uses naive pattern-by-pattern scanning.

Expected improvement: use `str.find` loops, regex lookahead, rolling hash, or Aho-Corasick while keeping exact counts.
