# knapsack_solver

Toy autoresearch project for 0/1 knapsack. Baseline uses a value/weight greedy heuristic, so it is fast but often not optimal.

Expected improvement: implement capacity-based dynamic programming in `train/train.py` / generated `submission/solver.py`.
