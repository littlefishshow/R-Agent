# Paper Research Scout References

This directory stores skill-local reference data for `paper_research_scout`.

- `read_papers.json`: durable list of papers already read by the user or imported from `outputs/papers/`.

Use `scripts/paper_read_history.py` to add, check, import, list, or filter records.
